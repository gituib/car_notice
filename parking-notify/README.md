# 停车通知系统

一个通过二维码实现的停车通知系统，支持多种通知方式（钉钉、飞书、企业微信、邮件、短信）。

## 功能特点

- 生成包含通知配置的加密二维码
- 扫码即可发送移车通知
- 支持多种通知方式
- 车主可自定义通知内容
- 扫码者可输入简短留言
- 彩色二维码样式
- 无需数据持久化，配置信息加密嵌入二维码

## 技术栈

- 后端：Python + Flask
- 前端：HTML5 + CSS3 + JavaScript
- 二维码生成：qrcode 库
- 加密：cryptography 库
- 通知集成：各平台 API

## 项目结构

```
parking-notify/
├── backend/                # 后端代码
│   ├── app/               # 应用主目录
│   │   ├── api/           # API 模块
│   │   ├── crypto/        # 加密模块
│   │   ├── qrcode/        # 二维码模块
│   │   ├── notifiers/     # 通知模块
│   │   ├── config/        # 配置模块
│   │   └── __init__.py
│   ├── run.py             # 应用启动入口
│   ├── requirements.txt   # Python 依赖
│   └── .env.example       # 环境变量示例
├── frontend/              # 前端代码
│   ├── public/            # 静态资源
│   │   ├── css/           # 样式文件
│   │   ├── js/            # JavaScript 文件
│   │   └── images/        # 图片资源
│   ├── templates/         # HTML 模板
│   └── index.html         # 前端入口
├── docs/                  # 文档
│   └── superpowers/
│       └── specs/         # 设计规范
└── README.md              # 项目说明
```

## 安装与部署

### 1. 后端安装

1. 进入后端目录
```bash
cd parking-notify/backend
```

2. 安装依赖
```bash
pip install -r requirements.txt
```

3. 配置环境变量
```bash
cp .env.example .env
# 编辑 .env 文件，设置 SECRET_KEY 等配置
```

4. 启动后端服务
```bash
python run.py
```

### 2. 前端部署

前端文件可以部署在任何静态文件服务器上，例如：

- Nginx
- Apache
- GitHub Pages
- Netlify

## 使用方法

### 1. 生成二维码

1. 访问前端生成二维码页面（默认为 `http://localhost:8000/index.html`）
2. 填写通知配置（钉钉、飞书、企业微信、邮件、短信等）
3. 自定义通知内容（可选）
4. 设置二维码颜色（可选）
5. 点击「生成二维码」按钮
6. 下载生成的二维码并打印贴在车上

### 2. 发送通知

1. 扫描车上的二维码
2. 在打开的页面中输入留言（可选）
3. 点击「发送通知」按钮
4. 系统会通过配置的方式发送通知给车主

## 通知方式配置

### 钉钉
1. 打开钉钉群，添加「自定义机器人」
2. 复制机器人的 Webhook URL
3. 在生成二维码页面填写该 URL

### 飞书
1. 打开飞书群，添加「机器人」
2. 选择「自定义机器人」
3. 复制机器人的 Webhook URL
4. 在生成二维码页面填写该 URL

### 企业微信
1. 打开企业微信群，添加「群机器人」
2. 复制机器人的 Webhook URL
3. 在生成二维码页面填写该 URL

### 邮件
1. 在 `.env` 文件中配置邮件服务器信息
2. 在生成二维码页面填写收件人邮箱

### 短信
1. 在 `.env` 文件中配置短信 API 信息
2. 在生成二维码页面填写手机号码

## 安全说明

- 使用 AES-256-GCM 加密算法保护配置信息
- 密钥存储在环境变量中，不硬编码
- 不存储任何车主信息
- 通知内容加密传输

## 未来扩展

- 支持更多通知方式（如微信小程序、电话通知等）
- 增加通知历史记录功能
- 实现多语言支持
- 添加验证码防止滥用

## 许可证

MIT License
